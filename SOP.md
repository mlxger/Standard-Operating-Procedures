## SOP行为检测系统方案



### 一、系统架构概览

| 层级       | 功能模块   | 技术要点                        |
| :--------- | :--------- | :------------------------------ |
| **感知层** | 人体检测   | 轻量级目标检测模型，ROI区域过滤 |
| **理解层** | 姿态估计   | 17个关键点提取（Top-Down方案）  |
| **决策层** | 状态机引擎 | 有限状态机（FSM）判定SOP步骤    |
| **应用层** | 告警与展示 | Web可视化平台，实时推送         |

#### 整体架构

```1c
┌─────────────┐
│  视频源接入   │  摄像头 / RTSP流 / 本地视频文件
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  帧提取 &    │  按固定帧率抽帧（通常5-10fps够用）
│  预处理      │  缩放、色彩空间转换
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  人体检测    │  检测画面中的操作人员（Bounding Box）
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  姿态估计    │  提取人体关键点（17个关节点）
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  状态机引擎  │  根据关键点序列，判定当前动作 → SOP步骤映射
└──────┬──────┘
       │
       ▼
─────────────┐
│  评估体系     │  DTW
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  告警 & 展示 │  Web可视化 / 声音报警 / 数据记录
└─────────────┘
```



### 二、核心技术方案

#### 2.1 人体检测层

- **模型选择**：轻量级检测模型（YOLO系列等），追求速度而非极致精度

- **ROI过滤**：只检测工位感兴趣区域内的人员，减少误检    【每个摄像头的感兴趣区域（Region of Interest）】

- **简单跟踪**：基于IoU匹配的跟踪器，稳定人员ID

  用的是轻量级目标检测模型，输入是摄像头画面，输出是每个人的边界框（Bounding Box）。选型上，我的建议是用最小的模型就够了——**模型越小，推理越快，部署越省心。** 工业场景不需要检测0.01像素大小的人，工位摄像头通常就对着1-2个工位，人在画面中占比较大，小模型完全够用。

  **技巧1：ROI区域过滤。** 每个工位的摄像头只关心自己工位区域的人，画面边缘的人可以忽略。我们通过配置文件定义每个摄像头的感兴趣区域（Region of Interest），只在ROI内做检测。这样既能减少误检，也能提升速度。

  **技巧2：稳定跟踪。** 检测框会有轻微抖动，需要加一个简单的跟踪算法来稳定ID。不用上那些复杂的多目标跟踪方案，简单的IoU匹配就够了——前后两帧检测框重叠度超过阈值就认为是同一个人。

#### 2.2 姿态估计层

| 案                        | 速度                 | 精度 | 适用场景              |
| :------------------------ | :------------------- | :--- | :-------------------- |
| Top-Down（先检测再估计）  | 较慢，人越多越慢     | 高   | 画面中人数少（1-3人） |
| Bottom-Up（先估计再分组） | 较快，跟人数关系不大 | 中等 | 画面中人数多          |
| 轻量级单人体方案          | 最快                 | 够用 | 单工位单人场景        |

- **方案**：Top-Down（先检测再估计），适合1-3人场景
- **输出**：17个关键点（头、肩、肘、腕、髋、膝、踝等）
- **坐标还原**：将裁剪区域的坐标映射回原图坐标系

#### 2.3 状态机行为判定（核心）

- **设计模式**：有限状态机（FSM）

  为什么用状态机而不是直接用分类模型？三个原因：

  1. **SOP流程天然就是有向的**——步骤1完了才能到步骤2，不存在从步骤1直接跳到步骤5的情况
  2. **状态机可以利用时序信息**——知道当前在哪个步骤，可以预判下一个合法步骤是什么
  3. **不需要大量训练数据**——不需要标注"步骤1的动作是什么样"，只需要定义每个状态的特征规则

- **状态转移**：严格按SOP顺序，禁止跳步/乱序

  1. **状态转移是有方向的**——只能按SOP顺序前进，不能跳跃
  2. **每个状态有超时机制**——卡太久说明有问题
  3. **违规有记录**——不合规的操作会留下"案底"
  4. **关键点驱动的判定**——用手的位置、头的朝向等特征判断动作

- **超时检测**：每个步骤设定最大允许时间

- **违规记录**：跳步、超时、漏检等行为自动记录

### 三、关键代码模块

| 模块                 | 功能         | 核心逻辑                    |
| :------------------- | :----------- | :-------------------------- |
| `SimpleTracker`      | 人员跟踪     | IoU匹配，维持人员ID稳定     |
| `PoseExtractor`      | 姿态提取     | 裁剪→推理→坐标还原          |
| `SOPStateMachine`    | 行为判定     | 状态转移+超时检测+违规记录  |
| `VideoSourceManager` | 多路视频管理 | RTSP/USB/文件接入，断线重连 |
| `FrameScheduler`     | 帧率控制     | 目标5fps，降低算力消耗      |

### 四、部署要点

| 坑点           | 问题描述                   | 解决方案                           |
| :------------- | :------------------------- | :--------------------------------- |
| **摄像头位置** | 太高看不清手，太低挡下半身 | 正前方2-2.5米，俯拍15-20度         |
| **光线问题**   | 频闪、逆光、分区开关灯     | 抗频闪相机、WDR、亮度检测模块      |
| **SOP变更**    | 客户频繁修改操作流程       | YAML配置文件化，5分钟切换          |
| **网络传输**   | WiFi不稳定、带宽不足       | 有线优先，或降分辨率+5GHz频段      |
| **算力瓶颈**   | 8路视频同时处理卡顿        | 降帧率(3-5fps)、分时处理、模型量化 |